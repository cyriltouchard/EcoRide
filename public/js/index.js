/**
 * EcoRide - Page d'accueil
 * Gestion de la logique spécifique à index.html
 * @file index.js
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('📱 Page d\'accueil initialisée');
    
    // Toute la logique spécifique à la page d'accueil peut être ajoutée ici
    // Pour l'instant, la page utilise uniquement les fonctionnalités communes
    // (navigation, animations, etc.) qui sont dans common.js
});
